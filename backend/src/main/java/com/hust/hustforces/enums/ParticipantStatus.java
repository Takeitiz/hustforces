package com.hust.hustforces.enums;

public enum ParticipantStatus {
    ACTIVE,      // Currently in the room
    IDLE,        // Connected but inactive
    DISCONNECTED,// Temporarily disconnected
    LEFT         // Permanently left the room
}