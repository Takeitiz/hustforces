package com.hust.hustforces.enums;

public enum UserStatus {
    ACTIVE,  // User has full access to the system
    SUSPENDED,  // User has temporary restricted access
    BANNED  // User has permanently restricted access
}
