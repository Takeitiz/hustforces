package com.hust.hustforces.enums;

public enum ParticipantRole {
    HOST,           // Room creator with full control
    COLLABORATOR,   // Can edit code and communicate
    VIEWER          // Can only view, no edit permissions
}
